const fs=require('fs');const path=require('path');const root=path.join(__dirname,'..');const app=fs.readFileSync(path.join(root,'public','app.js'),'utf8');const server=fs.readFileSync(path.join(root,'server.js'),'utf8');const pkg=require(path.join(root,'package.json'));const tokens=["exec_billing", "/api/saas/subscription", "Subscription & Billing"];const missing=tokens.filter(t=>!(app+server).includes(t));if(missing.length){console.error('HF18 FAIL',missing);process.exit(1)}if(pkg.nashCleanBuild.databaseSchemaTouched){console.error('Schema flag invalid');process.exit(1)}console.log('HF18 PASS',tokens.join(' | '));