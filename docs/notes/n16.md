# NASH OS — Clean Build 17 UI Compression / Navigation Cleanup Lock

## Scope
Compresses the visible navigation surface and introduces an auditable button matrix. It does not remove the operating centers; it reduces visible clutter and routes secondary capabilities through their source-labeled parent centers.

## Added UI
- UI Compression / Navigation Cleanup panel
- Compressed role surface summary
- Button-function matrix
- Hidden drill-down map
- UI compression evidence receipts

## Added Actions
- Load Clean Navigation
- Run Button Matrix
- Create UI Receipt
- Export Navigation Map

## Runtime API
- GET `/api/ui-compression/summary`
- GET `/api/ui-compression/surface`
- POST `/api/ui-compression/action`
- GET `/api/ui-compression/actions`

## Locks
- schemaMigrationIncluded = false
- databaseSchemaTouched = false
- humanFinalDecisionRequired = true
- aiAutonomousDecisionBlocked = true
- noDuplicatePages = true
- noSilentVisibleButtons = true
