# NASH OS Build 18B — Operational Shell Separation Lock

Purpose: correct the remaining UAT issue where Build 18A still exposed legacy build sections underneath the role command centers.

## Locked changes
- Role command center is now the default and only visible surface per role.
- Legacy Build 02–18 panels are hidden by default.
- Legacy centers open only as protected drill-downs after a command button requests them.
- Operational command packet appears inside the role command center after every action.
- Final Acceptance and UI Surface Audit remain available, but no longer dominate the normal workday surface.
- No schema migration.
- No database schema touch.
- Human final decision remains required.
- AI autonomous decisions remain blocked.

## Expected UAT result
Manager Home should show Manager Command Center only. Old Workday/Task/Closure/Quality/AI panels should not appear below it unless a command opens a drill-down.
